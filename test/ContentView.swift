//
//  ContentView.swift
//  test
//
//  Created by 郑建城 on 2025/2/8.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world war 3!")
        }
        .padding()
        .padding()
    }
}

#Preview {
    ContentView()
}
